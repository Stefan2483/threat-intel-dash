# Threat Intelligence Dashboard - Environment Variables
# Copy this file to .env.local for local development

# CORS Configuration (IMPORTANT FOR PRODUCTION)
# Comma-separated list of allowed origins for API requests
# Development: Use '*' to allow all origins
# Production: Replace with your actual domain(s)
# Example: https://threatintel.pro,https://www.threatintel.pro
ALLOWED_ORIGINS=*

# Node Environment
# Options: development, production, test
NODE_ENV=development

# Next.js Configuration
# Custom port (optional, default is 3000)
# PORT=3000

# RSS Feed Configuration (Optional)
# Maximum number of articles per feed (default: 5)
# MAX_ARTICLES_PER_FEED=5

# Cache duration in seconds (default: 300 = 5 minutes)
# CACHE_DURATION=300

# Rate Limiting (Optional)
# Maximum requests per window per IP (default: 30)
# RATE_LIMIT_MAX_REQUESTS=30

# Rate limit window in milliseconds (default: 60000 = 1 minute)
# RATE_LIMIT_WINDOW=60000

# Deployment Configuration
# Public site URL (for production builds)
# NEXT_PUBLIC_SITE_URL=https://threatintel.pro
